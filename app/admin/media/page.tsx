import {prisma} from "@/lib/prisma"; export default async function MediaAdmin(){const media=await prisma.media.findMany({orderBy:{createdAt:"desc"},take:50});return <div className="container py-16"><p className="kicker">الإعلام</p><h1 className="mt-3 text-4xl">الأرشيف الإعلامي</h1><div className="mt-10 grid gap-3">{media.map(x=><div key={x.id} className="border-t border-[var(--border)] py-5"><span className="text-sm text-[var(--muted)]">{x.type}</span><h2 className="mt-2 text-xl">{x.title}</h2></div>)}{!media.length&&<p className="py-10 text-[var(--muted)]">لا توجد مواد إعلامية بعد.</p>}</div></div>}