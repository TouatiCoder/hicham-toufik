"use client"; import {useState} from "react"; import {useRouter} from "next/navigation"; export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const r=useRouter();async function go(e:React.FormEvent){e.preventDefault();const x=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});if(x.ok)r.push("/admin");else setError("بيانات الدخول غير صحيحة");}return <div className="container grid min-h-[80vh] place-items-center"><form onSubmit={go} className="w-full max-w-md border border-[var(--border)] bg-[var(--surface)] p-8"><p className="kicker">ADMIN</p><h1 className="mt-3 text-3xl">تسجيل الدخول</h1><div className="mt-8 grid gap-4"><input type="email" placeholder="البريد الإلكتروني" value={email} onChange={e=>setEmail(e.target.value)} className="border border-[var(--border)] p-3 bg-transparent"/><input type="password" placeholder="كلمة المرور" value={password} onChange={e=>setPassword(e.target.value)} className="border border-[var(--border)] p-3 bg-transparent"/><button className="bg-[var(--ink)] p-3 text-white">دخول</button>{error&&<p className="text-sm text-red-700">{error}</p>}</div></form></div>}