import {NextResponse} from "next/server";import {prisma} from "@/lib/prisma";export async function GET(req:Request){const u=new URL(req.url);const q=u.searchParams.get("q")||undefined;const type=u.searchParams.get("type")||undefined;const items=await prisma.content.findMany({where:{status:"PUBLISHED",...(type?{type}:{ }),...(q?{OR:[{title:{contains:q,mode:"insensitive"}},{excerpt:{contains:q,mode:"insensitive"}}]}:{})},orderBy:{publishedAt:"desc"},take:50,include:{category:true}});return NextResponse.json(items)}