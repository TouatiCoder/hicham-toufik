import {NextResponse} from "next/server"; import {prisma} from "@/lib/prisma";
export async function POST(req:Request){try{const b=await req.json();if(!b.title||!b.slug)return NextResponse.json({error:"العنوان والرابط مطلوبان"},{status:400});const item=await prisma.content.create({data:{title:b.title,slug:b.slug,type:b.type||"ARTICLE",excerpt:b.excerpt||null,body:b.body||null,year:b.year||null,status:b.status==="DRAFT"?"DRAFT":"PUBLISHED",publishedAt:b.status==="DRAFT"?null:new Date()}});return NextResponse.json(item,{status:201})}catch(e){return NextResponse.json({error:"تعذر حفظ المادة"},{status:500})}}