"use client";import {useState} from "react";export default function Search(){const[q,setQ]=useState("");const[items,setItems]=useState<any[]>([]);async function run(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/content?q="+encodeURIComponent(q));setItems(await r.json())}return <div className="container py-20"><p className="kicker">البحث</p><h1 className="mt-4 text-5xl">ابحث في الأرشيف</h1><form onSubmit={run} className="mt-10 flex gap-3"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="كتاب، بحث، دراسة، مقابلة…" className="min-w-0 flex-1 border border-[var(--border)] bg-[var(--surface)] p-4"/><button className="bg-[var(--ink)] px-7 text-white">بحث</button></form><div className="mt-12 border-t border-[var(--border)]">{items.map(x=><article key={x.id} className="border-b border-[var(--border)] py-7"><span className="text-sm text-[var(--muted)]">{x.type}</span><h2 className="mt-2 text-2xl">{x.title}</h2><p className="mt-2 text-[var(--muted)]">{x.excerpt}</p></article>)}</div></div>}